-- Migration 1: add CacheMetadata table (introduced after initial schema)

CREATE TABLE IF NOT EXISTS CacheMetadata(
    id INTEGER NOT NULL PRIMARY KEY,
    lastCountriesSyncEpochMs INTEGER
);

-- Ensure the single-row record exists (optional)
INSERT OR IGNORE INTO CacheMetadata(id, lastCountriesSyncEpochMs) VALUES (1, NULL);

